extends layout

block stylesheets
  link(rel='stylesheet', href='/reviews.bundle.css')

block content
  include partials/header
  .reviews.container
    .reviews-container
      each review in reviews
        .review(data-event=review)
          .image
            img(src='https://baconmockup.com/200/200')
          .text
            h4= review.quote
            cite= review.name

      else
        .event
          h3 NOTHING HERE. 
          p This world is a vacuous empty pit of despair with nothing novel or exciting ever happening.

  include partials/swansonquote

block scripts
  script(src='/reviews.bundle.js')
